apiVersion: v1
kind: Service
metadata:
    name: "bjinazure01-69b7"
    labels:
        app: "bjinazure01-69b7"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "bjinazure01-69b7"